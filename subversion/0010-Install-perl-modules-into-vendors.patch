From 8e42fad9c9b2bd1d59b85e27b67a33b9a23d9554 Mon Sep 17 00:00:00 2001
From: David Rothenberger <d.rothenberger@entomo.com>
Date: Thu, 7 Nov 2013 23:02:43 +0400
Subject: [PATCH 10/21] Install perl modules into vendors

---
 Makefile.in | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/Makefile.in b/Makefile.in
index eac8bdb..43210aa 100644
--- a/Makefile.in
+++ b/Makefile.in
@@ -882,7 +882,7 @@ $(SWIG_PL_DIR)/native/Makefile.PL: $(SWIG_SRC_DIR)/perl/native/Makefile.PL.in
 	./config.status subversion/bindings/swig/perl/native/Makefile.PL
 
 $(SWIG_PL_DIR)/native/Makefile: libsvn_swig_perl $(SWIG_PL_DIR)/native/Makefile.PL
-	cd $(SWIG_PL_DIR)/native; $(PERL) Makefile.PL PREFIX=$(prefix)
+	cd $(SWIG_PL_DIR)/native; $(PERL) Makefile.PL PREFIX=$(prefix) INSTALLDIRS=vendor
 
 # There is a "readlink -f" command on some systems for the same purpose,
 # but it's not as portable (e.g. Mac OS X doesn't have it).  These should
-- 
2.37.2

